let string = "Hello world!";

const TypeOfMethods = (str) => {
    return str.repeat(2)
    /*El método repeat() construye y devuelve 
    una nueva cadena que contiene el número especificado
    de copias de la cadena en la cual fue llamada, 
    concatenados.*/
} 

console.log(TypeOfMethods(string)); 



